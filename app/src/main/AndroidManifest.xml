<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rucafe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RUCafe">
        <activity android:name=".DonutOrderActivity" android:label="@string/donut_activity_title"
            android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".StoredOrdersActivity"
            android:label="@string/stored_orders_activity_title"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".CurrentOrderActivity"
            android:label="@string/current_order_activity_title"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".CoffeeOrderActivity" android:label="@string/coffee_activity_title"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>